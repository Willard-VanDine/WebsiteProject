import { Account } from "../model/account.interface";

export class AccountService {
    private acc : Account = {
        username : "", 
        accountWins : 0, 
        accountLosses : 0
    };
    
}